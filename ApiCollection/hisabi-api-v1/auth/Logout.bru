meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/auth/logout
  body: none
}

headers {
  Authorization: Bearer {{token}}
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("token", "", { persist: true });
  }
}

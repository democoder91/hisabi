meta {
  name: Expenses By Account
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v1/metrics/expenses-by-account?from={{fromDate}}&to={{toDate}}
  body: none
}

headers {
  Authorization: Bearer {{token}}
}
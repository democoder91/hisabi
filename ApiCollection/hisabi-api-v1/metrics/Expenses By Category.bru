meta {
  name: Expenses By Category
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v1/metrics/expenses-by-category?from={{fromDate}}&to={{toDate}}
  body: none
}

headers {
  Authorization: Bearer {{token}}
}
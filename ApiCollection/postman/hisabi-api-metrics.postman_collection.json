{
    "info": {
        "_postman_id": "0e1de95a-b524-4e45-9f29-5ed5e2f1c101",
        "name": "Hisabi API Metrics",
        "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json",
        "version": "1.0.0"
    },
    "item": [
        {
            "name": "Total Income",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/total-income?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Total Expenses",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/total-expenses?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Total Assets",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/total-assets?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Total Liabilities",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/total-liabilities?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Total Equity",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/total-equity?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Net Worth",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/net-worth"
            }
        },
        {
            "name": "Net Worth Trend",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/net-worth-trend?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Total Income Trend",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/total-income-trend?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Total Expenses Trend",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/total-expenses-trend?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Account Trend",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/account-trend?from={{fromDate}}&to={{toDate}}&id={{accountId}}"
            }
        },
        {
            "name": "Account Daily Trend",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/account-daily-trend?from={{fromDate}}&to={{toDate}}&id={{accountId}}"
            }
        },
        {
            "name": "Expenses By Account",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/expenses-by-account?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Income By Account",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/income-by-account?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Transactions Count",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/transactions-count?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Highest Transaction",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/highest-transaction?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Lowest Transaction",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/lowest-transaction?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Average Transaction",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/average-transaction?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Account Stats",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/account-stats?from={{fromDate}}&to={{toDate}}"
            }
        },
        {
            "name": "Circle Pack",
            "request": {
                "method": "GET",
                "header": [
                    {
                        "key": "Authorization",
                        "value": "Bearer {{token}}"
                    }
                ],
                "url": "{{baseUrl}}/api/v1/metrics/circle-pack?from={{fromDate}}&to={{toDate}}"
            }
        }
    ]
}